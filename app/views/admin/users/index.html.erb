<h1>List of Users</h1>
<% @users.each do |user| %>
  <%= link_to "#{user.username}" %>
  <% if user.active == true %>
    <%= button_to 'Disable' %>
  <% else %>
    <%= button_to 'Enable' %>
  <% end %>
<% end %>
